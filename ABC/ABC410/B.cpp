#include <bits/stdc++.h>
#include <atcoder/all>
using namespace std;
using namespace atcoder;
using ll = long long;

int main() {
    int n,q;
    cin >>n>>q;
    vector<int> x(q,0);
    vector<int> b(q,0);
    for (int i = 0; i < q; i++)
    {
        cin>>x[i];
    }
    
}